package org.aituition.pdfbox.api.layout.elements.render;

import org.aituition.pdfbox.api.layout.elements.Document;

/**
 * Each element in a document is
 * {@link Document#add(org.aituition.pdfbox.api.layout.elements.Element, LayoutHint)
 * accompanied} by a hint, which gives some notes to the current layout on how
 * to layout the element.
 */
public interface LayoutHint {

}
